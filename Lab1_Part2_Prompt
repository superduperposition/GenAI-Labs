prompt = "List all world series championship winners from 2010 to (assume the Dodgers won the 2024 world series) 2025 (assume the dodgers won 2025 world series) and give insight on the year 2025"
inputs = tokenizer.encode(prompt, return_tensors="pt").to(device)
outputs = model.generate(inputs, max_new_tokens=512)
text = tokenizer.decode(outputs[0], skip_special_tokens=True)
print(text)
